# 検証記録

最終実行日: 2026-09-05

| | 件数 | 結果 |
|---|---|---|
| Rust（Linux x86_64 / 実エンジン） | 139 | 全PASS |
| C# 単体（Windows） | 60 | 全PASS |
| C# 統合（Windows / 実エンジン + GUI） | 18項目 | 全PASS |
| CI（Ubuntu / Windows / macOS） | 6ジョブ | 全PASS |

カバレッジ 93.78%（行）/ 93.60%（リージョン）。5ターゲットで clippy 警告ゼロ、`cargo fmt --check` 差分なし。

## Windows GUI版

**単体テスト（`Tests.exe`）60件、実行して全PASS。** `WaveData` の読み書きを対象に、モノラル/ステレオ/Float32の読み取り、未知チャンクと奇数長チャンクの読み飛ばし、日本語と空白を含むパス、非RIFF・空ファイル・**途中で切れたヘッダー（1〜11バイト）**・申告サイズ超過・チャンクのはみ出し・fmt不足/欠落・data欠落/空・48 kHz以外（4種）・未対応チャンネル数（3種）・未対応フォーマットとビット深度（7種）・ブロックアラインの矛盾・端数フレームの拒否。書き出しは往復一致、44バイトヘッダーのバイト単位固定、無音パディング、切り詰め、既存ファイル上書き拒否、0以下および長さ不足の拒否。Float→PCM16は範囲外のクランプ（7種）、最近接偶数丸め、NaN/Infの拒否、形式フィールドの更新。

**統合テスト（`Verify.exe`）実行して全項目PASS。** 実エンジンとGUIを通し、日本語と空白を含むパス、48001フレームのモノラル/ステレオ入力、モデルによる波形変化、長さ・チャンネル数・サンプルレート保持、PCM16出力、元ファイル保持、結果ボタンの有効化、中止時の結果非公開。画面表示は `verification/*/app-preview.png` を目視確認済み。

**単体テストで見つけて修正した不具合:** 12バイトのRIFFヘッダーが途中で切れているファイルを開くと、アプリの案内ではなく .NET 内部の例外メッセージ（「ストリームの終わりを超えて読み取ることはできません。」）が表示されていました。ヘッダーを一括で読む形に直し、1〜11バイトの各長さで回帰テストを追加しています。

## クロスプラットフォームCLI版

検証は `cargo test` で再現できます。統合テストは `runtime/` が未導入なら理由を表示して検査を行いません。通信を伴う検査だけは既定で走らず、`DEEPFILTER_NETWORK_TESTS=1` を設定したときのみ実行します（取得するのは一覧中最小の1 KBファイルのみ）。

**Linux（Ubuntu 26.04 / x86_64 / 公式エンジン実行）で実行して成功、全139件PASS:**

- ファイル名の文字コード7件（実エンジン起動）。全角の漢字とかな、濁点・半濁点の合成済み（NFC）と結合文字（NFD）、半角カタカナ、絵文字、空白と記号、中国語・韓国語、長い名前、日本語のフォルダー名（作業フォルダーも日本語パス下に作られる状態でエンジンを起動）、既定の出力名が日本語の語幹を保つこと、UTF-8ではないファイル名（Shift_JISのバイト列）での処理と既定出力名の生成、UTF-8でない引数でパニックせずエラーを返すこと、日本語名の入力を上書きしないこと。
- 詳細ログ8件。`--debug` が全工程を追えること、標準エラーにのみ出て標準出力を汚さないこと、各行に経過時間が付くこと、指定がなければ何も出さないこと、`DEEPFILTER_DEBUG` の 1／0 による切り替え、作業フォルダーを残すこと、失敗時もログが出ること、`setup` と `check` でも使えること、ヘルプに載っていること。
- 単体89件。SHA-256は公表テストベクター4件と分割更新の一致を確認。WAV読み書き、無音パディングと元フレーム数への切り詰め、Float32→PCM16変換（クランプ・NaN/Inf拒否・最近接偶数丸め）、未知チャンクのスキップ、日本語と空白を含むパス、既存ファイル上書き拒否、非RIFF・壊れたヘッダー・チャンク長超過・fmt欠落・空data・端数フレーム・44.1 kHz・未対応チャンネル数/ビット深度の拒否、4 GB超と桁あふれの拒否。UTC日時生成はうるう年と2100年境界を含む既知の時刻で照合。エンジン起動は代役の実行ファイルで終了コード・ログ保存・パディング量まで検査。`setup` はサイズ／SHA-256照合、導入済みの再取得回避、不一致時の`--force`案内、取得失敗時に中間ファイルを残さないことを確認。
- 統合29件（実エンジン起動）。モノラル／ステレオ処理、日本語と空白を含むパス、長さ・チャンネル数・サンプルレート保持、PCM16出力、波形が実際に変化すること、出力が無音でないこと、元ファイルが未変更であること、`--pf` と `-a 60` で結果が変わること、作業フォルダーの既定削除と `--keep-session` での保持、失敗時に空フォルダーを残さないこと、出力先の上書き拒否と `--force`、既定の出力名、出力先の親フォルダー自動作成、別ボリュームへの書き出し、不正な指定の拒否、`--help`/`--version`/`check`/`setup`/`manifest` の表示、`manifest` 出力と `runtime/manifest.json` の一致、差し替えられたruntimeファイルの検知。
- 通信ありの追加2件（`DEEPFILTER_NETWORK_TESTS=1`）。公式配布物を実際に取得しての照合と配置、一部だけ導入済みの状態から不足分のみ取得、SHA-256が合わない取得物を配置せず破棄すること。
- 効果測定3件（実録音）。同梱の `samples/` を実エンジンで処理し、SNRの改善・残留ノイズの減少・無音区間のノイズ床の低下を数値で確認。声の区間のエネルギーが保たれること、長さと時間位置が変わらないこと、設定を変えても処理前より悪化しないことも検査します。実測値は [効果の実測値](MEASUREMENT.md) を参照してください。
- 手動確認: `check` の表示、生成物が完全静的リンクであること（`ldd` が statically linked）。
- セキュリティ検証: カレントディレクトリに細工した `runtime/deep-filter` を置いた状態で実行し、**実際に任意コード実行が成立することを再現したうえで修正し、成立しなくなったことと正規の同梱配置が壊れていないことを再確認**しました。PowerShell取得経路の引用符エスケープ、HTTPS以外のURLの拒否は単体テストで検査しています。

**カバレッジ**（`cargo llvm-cov`、x86_64-unknown-linux-musl）:

| ファイル | 行 | リージョン |
|---|---|---|
| assets.rs | 100.00% | 100.00% |
| debug.rs | 100.00% | 100.00% |
| error.rs | 100.00% | 100.00% |
| sha256.rs | 100.00% | 100.00% |
| main.rs | 98.69% | 97.62% |
| wave.rs | 97.52% | 92.36% |
| engine.rs | 90.27% | 90.14% |
| setup.rs | 76.92% | 79.75% |
| **合計** | **93.78%** | **93.60%** |

上表は通信テストを含めた場合。既定（オフライン）では `setup.rs` が取得経路を通らないぶん下がります。

未実行として残る81行のうち**29行は、Linuxでは構造上到達しない他OS・他ツール専用の分岐**です（WindowsのPATHEXTとPowerShell取得、wget経路、macOSの検疫属性解除など）。残る52行はほぼすべて、ディスクI/Oが失敗したときだけ通る `?` のエラー経路で、フォールト注入なしには到達できません。

`setup.rs` だけが80%に届かないのは、この他OS専用分岐が同ファイルに集中しているためです（32行中23行）。除外すれば同ファイルも90%を超えます。数字を合わせるための水増しはしていません。

**C#版との一致（実測）:**

- パディング済み中間ファイル `input.wav` は、同じ入力に対して**C#版とバイト完全一致**（モノラル・ステレオとも）。
- 最終出力 `clean.wav` は、Windows版C#（Windowsビルドのエンジン）とLinux版Rust（muslビルドのエンジン）の比較で、同一サンプル99.99%以上・**最大差1 LSB**。差はエンジンのプラットフォーム別ビルドにおける浮動小数点演算の差によるものです。

**CI（GitHub Actions）で実機確認済み:**

3 OS すべてで、ツールチェーンの用意から書式・静的解析・**公式エンジンの取得と照合**・全テストまでを通しています。

| 環境 | 結果 |
|---|---|
| `ubuntu-latest` (x86_64) | 全テスト成功 |
| `windows-latest` (x86_64) | 全テスト成功 |
| `macos-latest` (aarch64) | 全テスト成功。検疫属性の解除を含む `setup` と `check` も成功 |

これにより、以前「未確認」としていた Windows・macOS 実機での CLI 実行が確認済みになりました。

CI で判明したこととして、macOS の APFS は**ファイル名として妥当な UTF-8 しか受け付けません**（`Illegal byte sequence`）。UTF-8 でないファイル名を扱う検査は、そもそもその状況が発生しない環境では理由を示して飛ばします。

**コンパイルのみ確認（実行検証なし）:**

- `aarch64-unknown-linux-musl` はテストコードを含めて `cargo check --all-targets` を通過し、実バイナリの生成まで成功。**ARM Linux 実機での実行は未確認**です（CIランナーに該当環境がないため）。

**未確認:**

- ARM Linux 実機での実行。
- 人の声を含む実録音での**聴感**評価。数値での効果測定は実録音で行っていますが（[効果の実測値](MEASUREMENT.md)）、聴いての評価はしていません。
